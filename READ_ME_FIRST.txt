Place the PDF file named exactly 'INFORMATION ETHICS.pdf' in the same folder as app.py before running:
pip install -r requirements.txt
streamlit run app.py

The app will render a snapshot of the cited page inside an expander.